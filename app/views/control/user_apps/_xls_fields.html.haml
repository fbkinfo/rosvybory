
%tr.nested-fields
  - ExcelUserAppRow.column_names.each do |i|
    %td{:class => i}
      = f.text_field i
      = content_tag(:div, f.object.errors[i].to_sentence) if f.object.errors[i].present?
  %td
    = link_to_remove_association 'Удалить', f
